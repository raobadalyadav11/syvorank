import { SiteFrame, CTA, eyebrow, section } from '@/components/marketing'
export function StandardPage({title, kicker, intro, cards}:{title:string;kicker:string;intro:string;cards:[string,string][]}){return <SiteFrame><main><section className={`${section} py-24`}><p className={eyebrow}>{kicker}</p><h1 className="mt-5 max-w-4xl text-balance text-5xl font-semibold tracking-tight md:text-6xl">{title}</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-muted-foreground">{intro}</p></section><section className={`${section} grid gap-5 pb-20 md:grid-cols-3`}>{cards.map(([t,d])=><article key={t} className="rounded-2xl border border-border p-7"><h2 className="text-xl font-semibold">{t}</h2><p className="mt-3 leading-7 text-muted-foreground">{d}</p></article>)}</section><CTA /></main></SiteFrame>}
