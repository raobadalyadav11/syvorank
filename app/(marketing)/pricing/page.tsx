'use client'
import { useState } from 'react'
import Link from 'next/link'
import { SiteFrame, eyebrow, section } from '@/components/marketing'
export default function Pricing(){const [annual,setAnnual]=useState(true);const plans=[['Starter',annual?'$79':'$99','For focused teams building their first repeatable growth motion.'],['Growth',annual?'$249':'$299','For teams ready to connect visibility to pipeline.'],['Scale',annual?'$599':'$699','For multi-market programs with ambitious targets.']];return <SiteFrame><main><section className={`${section} py-24 text-center`}><p className={eyebrow}>Simple, transparent pricing</p><h1 className="mt-5 text-balance text-5xl font-semibold tracking-tight md:text-6xl">Choose the momentum that fits.</h1><p className="mx-auto mt-6 max-w-xl text-lg leading-8 text-muted-foreground">Start with the essentials. Add intelligence as your growth motion compounds.</p><div className="mt-8 inline-flex rounded-full border border-border p-1"><button onClick={()=>setAnnual(true)} className={`rounded-full px-4 py-2 text-sm ${annual?'bg-primary text-primary-foreground':''}`}>Annual <span className="text-xs opacity-70">save 20%</span></button><button onClick={()=>setAnnual(false)} className={`rounded-full px-4 py-2 text-sm ${!annual?'bg-primary text-primary-foreground':''}`}>Monthly</button></div></section><section className={`${section} grid gap-5 pb-24 lg:grid-cols-3`}>{plans.map(([name,price,desc],i)=><article key={name} className={`rounded-2xl border p-8 ${i===1?'border-primary bg-primary/[0.04] shadow-xl shadow-primary/10':'border-border'}`}><h2 className="text-xl font-semibold">{name}</h2><p className="mt-4 text-4xl font-semibold">{price}<span className="text-sm font-normal text-muted-foreground"> / month</span></p><p className="mt-4 min-h-14 leading-7 text-muted-foreground">{desc}</p><Link href="/contact" className="mt-8 block rounded-lg bg-primary px-4 py-3 text-center text-sm font-medium text-primary-foreground">Get started</Link><ul className="mt-8 flex flex-col gap-3 text-sm text-muted-foreground"><li>✓ Daily visibility monitoring</li><li>✓ Opportunity recommendations</li><li>✓ Content workspace</li></ul></article>)}</section></main></SiteFrame>}
